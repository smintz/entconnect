---
phase: 03-validation-fidelity
plan: 07
type: execute
wave: 5
gap_closure: true
depends_on: [03-03, 03-04]
files_modified:
  - Makefile
  - scripts/pipeline.sh
  - .github/workflows/ci.yml
  - mixinforproto/violation_test.go
autonomous: true
requirements: [VAL-06, VAL-07, PIPE-05]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "VAL-07's single-ValidationError-construction-site invariant is enforced by a runnable gate, not merely asserted in a comment: a second production construction site makes the gate exit non-zero (ROADMAP Phase 3 SC2 / WR-07)"
    - "The gate is reachable three ways that cannot drift apart: a named Makefile target, the canonical pipeline script, and a CI step — wired the same way `check-dep-parity` already is (VAL-11's precedent, D-15)"
    - "The gate distinguishes a production construction site from a test fixture and from a comment mentioning the type, so `runtime/errormap_test.go`'s two legitimate fixture constructions and `violation_test.go`'s own doc comment do not make it red"
    - "`mixinforproto/violation_test.go`'s comment names the gate that actually exists — no test comment claims an enforcement mechanism the repository does not contain"
    - "Running the gate against a scratch copy carrying a deliberately-introduced second production construction site exits non-zero, and that rehearsal is recorded in the SUMMARY (Phase 1 D-25's every-failure-path-is-rehearsed discipline)"
  prohibitions:
    - "MUST NOT close a red coverage, parity, or invariant guard by deleting the assertion, weakening its comparison, or adding an entry to its exception list without a written design reason — a guard failure is closed by adding coverage"
  artifacts:
    - Makefile
    - scripts/pipeline.sh
    - .github/workflows/ci.yml
  key_links:
    - "Makefile check-single-validationerror-site -> scripts/pipeline.sh -> .github/workflows/ci.yml — the three-way wiring `check-dep-parity` established"
    - "mixinforproto/violation_test.go:262-269 -> the Makefile target it names — currently a dangling reference"
---

<objective>
Close verification gap **WR-07** from `03-VERIFICATION.md`: `mixinforproto/violation_test.go`
lines 262-269 cite "Makefile's grep-based check for the authoritative, whole-repo version
of this assertion" as the enforcement mechanism behind VAL-07's identity invariant. **No
such target exists.** The `.PHONY` list is `build vet test test-determinism
test-standalone check-modules check-stubs check-goversion check-dep-parity pipeline`, and
`grep -rn 'protovalidate.ValidationError{' Makefile` is empty.

The verifier's own rationale for preferring the gate over correcting the comment: this
invariant is the one structural defense against a second construction site silently
reintroducing the whole CR-01/CR-02/CR-03 bug class. D-01 makes protovalidate's own
`*protovalidate.ValidationError` the public error type; D-07 consequence 3 requires error
construction to be single-pathed **even though evaluation is not**. A second construction
site is precisely how two evaluation paths stop agreeing without anyone noticing.

Purpose: turn a comment that lies into a gate that fails.
Output: a `check-single-validationerror-site` Makefile target, wired into `pipeline` and
CI exactly as `check-dep-parity` is, plus a corrected test comment naming the real target.

This plan touches no Go production code and shares no file with `03-06-PLAN.md`, so the
two run in parallel in wave 5.
</objective>

<execution_context>
@/home/user/entconnect/.claude/gsd-core/workflows/execute-plan.md
@/home/user/entconnect/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-validation-fidelity/03-VERIFICATION.md
@.planning/phases/03-validation-fidelity/03-04-SUMMARY.md
</context>

<interface_context>
Facts the executor needs and must not re-derive:

- **The one legitimate production construction site is `mixinforproto/violation.go:64`**,
  which returns `&protovalidate.ValidationError{Violations: out}`.
- A repo-wide search for `protovalidate.ValidationError{` today returns exactly four hits:
  - `mixinforproto/violation.go:64` — the production constructor (the invariant's subject)
  - `runtime/errormap_test.go:20` and `:52` — legitimate **test fixtures** in the root
    module, constructing a `ValidationError` to feed `MapError`. These must NOT make the
    gate red.
  - `mixinforproto/violation_test.go:264` — the literal appearing inside the **doc comment**
    this plan corrects. Also must not make the gate red.
- `check-dep-parity` (`Makefile:166-181`) is the wiring precedent: a `.PHONY` entry, a
  `## `-prefixed doc block above the target, a `fail=0` / `exit $$fail` accumulator body,
  and a CI step under the `standalone` job (`.github/workflows/ci.yml:122-123`).
- `scripts/pipeline.sh` is `set -euo pipefail` and SERIAL-ONLY; every step is announced by
  name and a genuine no-op is a success, never a silent skip.
- `MODULES := . ./mixinforproto` (`Makefile:10`) — the explicit, checked-in module list.
  Both modules must be in scope of the gate; a root-only `./...` walk does not descend
  into a nested `go.mod` (PITFALLS.md Pitfall 12).
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: The check-single-validationerror-site Makefile target, with both directions rehearsed</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (the whole file — `MODULES` at line 10, the `.PHONY` list at line 12, and `check-dep-parity` at 134-181 as the shape to mirror)
    - mixinforproto/violation.go (confirm the exact production construction expression at line 64)
    - runtime/errormap_test.go lines 15-60 (the two legitimate test-fixture constructions the gate must tolerate)
    - mixinforproto/violation_test.go lines 260-285 (the comment containing the literal, which the gate must also tolerate)
    - .planning/phases/03-validation-fidelity/03-VERIFICATION.md lines 50-59 (the gap's own `missing:` bullet)
  </read_first>
  <action>
    Add a `check-single-validationerror-site` target to the Makefile and add its name to
    the `.PHONY` list at line 12, keeping that list's existing order and appending before
    `pipeline`.

    **What the gate asserts.** Across every `.go` file in the repository, excluding files
    whose name ends in `_test.go`, and with `//`-prefixed comment lines stripped, the
    literal `protovalidate.ValidationError{` must appear in exactly one file, and that file
    must be `mixinforproto/violation.go`. Any other count, or any other path, is a
    blocking failure.

    **Why both filters are required, and neither is optional.** Without the `_test.go`
    exclusion the gate is red on day one — `runtime/errormap_test.go` legitimately
    constructs a `ValidationError` twice to exercise `runtime.MapError`, and forbidding
    that would forbid testing the error-mapping path at all. Without comment stripping the
    gate is self-invalidating: `mixinforproto/violation_test.go:264` mentions the literal
    inside prose, and any future production-file doc comment naming the type would turn a
    documentation improvement into a red build. Record both reasons in the `## `-prefixed
    doc block above the target, in the same explanatory register `check-dep-parity`'s own
    doc block uses.

    **Failure message.** On failure, print every offending path with its line, then state
    the invariant and its source: D-01 makes `*protovalidate.ValidationError` this
    package's public error type and D-07 consequence 3 requires error construction to be
    single-pathed even though evaluation is not; a second construction site is how the two
    evaluation paths stop agreeing silently. Point the reader at
    `mixinforproto/violation.go`'s `newValidationError` as the one place to route through.
    Follow Phase 1 D-08's discipline: the first line must be self-sufficient.

    **Scope.** Walk the repository from the root rather than per-module, since the
    invariant is whole-repo by definition — but exclude generated trees
    (`mixinforproto/internal/gen/`, any `ent/` directory produced by entc) so a future
    generator emitting the type cannot make the gate mysteriously red without a human
    decision; name each exclusion in the doc block.

    **Rehearse both directions in a scratch copy, never in the working tree.** Copy the
    repository to a temp directory, introduce a second production construction site in a
    non-test `.go` file, run the target, confirm it exits non-zero and names that file;
    then remove it and confirm the target exits 0. Record both observed outputs verbatim in
    the SUMMARY. This is Phase 1 D-25's "every failure path gets a test asserting the
    specific message, not merely that it failed" applied to a Makefile gate, and it is the
    same rehearsal discipline `03-04-SUMMARY.md` records for `check-dep-parity`.
  </action>
  <verify>
    <automated>make check-single-validationerror-site</automated>
    <automated>grep -c 'check-single-validationerror-site' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `make check-single-validationerror-site` exits 0 against the current working tree.
    - The target name appears in the Makefile's `.PHONY` list (`grep -n '^\.PHONY' Makefile` output contains `check-single-validationerror-site`).
    - Run in a scratch copy with a second production construction site added: the target exits non-zero and its output names the offending file path. The observed output is pasted into the SUMMARY.
    - Run in that same scratch copy after removing the added site: the target exits 0.
    - The target's output is unchanged (still exit 0) when a `//` comment mentioning the type is added to a production `.go` file in a scratch copy — proving the comment-stripping filter works, not merely that it was written.
    - The target's output is unchanged (still exit 0) with `runtime/errormap_test.go` untouched — proving the `_test.go` exclusion works against the two real fixture constructions already in the tree.
    - The `## ` doc block above the target states the `_test.go` exclusion reason, the comment-stripping reason, and each generated-tree exclusion by path.
  </acceptance_criteria>
  <done>`make check-single-validationerror-site` passes on a clean tree, fails loudly and by name on a planted second production construction site, and both directions are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the gate into pipeline and CI, and correct the comment that named a target that did not exist</name>
  <files>scripts/pipeline.sh, .github/workflows/ci.yml, mixinforproto/violation_test.go</files>
  <read_first>
    - Makefile (post-Task-1 state — the exact target name and its doc block)
    - scripts/pipeline.sh (the whole file — the fixed five-step order, the step-announcement convention, and the `set -euo pipefail` abort semantics)
    - .github/workflows/ci.yml lines 84-125 (the `standalone` job and the `check-dep-parity` step at 122-123 — the wiring precedent)
    - mixinforproto/violation_test.go lines 260-285 (the comment to correct and the test it documents)
  </read_first>
  <action>
    Make the gate reachable the same three ways `check-dep-parity` is, so the three call
    sites cannot drift apart — the failure mode `scripts/generate-stubs.sh` exists to
    prevent for stub generation (VERIFICATION.md gap 4 / REVIEW.md CR-04 in Phase 1).

    **pipeline.sh.** Add a step invoking `make check-single-validationerror-site`. Do NOT
    reorder or renumber the existing five steps — the script's own header calls that order
    "fixed, non-negotiable". Append the invariant check as a new, named step after the
    existing ones, announced by name in the same `[pipeline]` echo style, and update the
    header comment block's numbered list so the file's own documentation matches what it
    runs. Because the script is `set -euo pipefail`, a red gate aborts the pipeline at that
    point, which is the intended behavior.

    **ci.yml.** Add a step named for the invariant, running
    `make check-single-validationerror-site`, in the job where it belongs. Choose the job
    deliberately and record the choice in the SUMMARY: unlike `check-dep-parity`, this gate
    has no `GOWORK=off` requirement — it is a source-text invariant with no module
    resolution at all — so the `modules` job that already runs `make build`/`make vet`/
    `make test` is the natural host, and adding it to the `standalone` job would imply a
    workspace-resolution dependency that does not exist. Do not add it to both.

    **violation_test.go.** Correct the doc comment at lines 262-269 so it names the target
    that now exists: replace the vague "Makefile's grep-based check" phrasing with the
    literal target name and the command a reader can run. The comment's substantive claim —
    that `TestNewValidationError_SingleBuilderSite` is a lightweight structural pin and the
    Makefile target is the authoritative whole-repo assertion — becomes true once the
    target exists; keep that framing and make the reference concrete. Do not change the
    test body.

    Note for the executor: this file is also read by `03-06-PLAN.md`'s tasks but is not in
    that plan's `files_modified` — it is owned by this plan alone in wave 5.
  </action>
  <verify>
    <automated>bash -n scripts/pipeline.sh</automated>
    <automated>grep -c 'check-single-validationerror-site' scripts/pipeline.sh .github/workflows/ci.yml mixinforproto/violation_test.go</automated>
    <automated>cd mixinforproto &amp;&amp; go test ./... -run TestNewValidationError_SingleBuilderSite -v</automated>
    <automated>make check-single-validationerror-site</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'check-single-validationerror-site' scripts/pipeline.sh` is at least 1.
    - `grep -c 'check-single-validationerror-site' .github/workflows/ci.yml` is exactly 1 — the gate is wired into one job, not duplicated across `modules` and `standalone`.
    - `grep -c 'check-single-validationerror-site' mixinforproto/violation_test.go` is at least 1 — the comment names a target that exists.
    - `bash -n scripts/pipeline.sh` exits 0.
    - The numbered step list in `scripts/pipeline.sh`'s header comment includes the new step; the count of steps described equals the count of steps executed.
    - `cd mixinforproto && go test ./... -run TestNewValidationError_SingleBuilderSite` exits 0 with the test body unchanged (`git diff` on that file shows comment-only changes).
    - `make check-single-validationerror-site` still exits 0 after the comment edit — the comment-stripping filter tolerates the corrected prose, which itself mentions the type.
    - The SUMMARY records which CI job hosts the step and why the `standalone` job was not chosen.
  </acceptance_criteria>
  <done>The invariant gate runs from `make`, from `scripts/pipeline.sh`, and from CI; `violation_test.go`'s comment names the real target; no test comment in the repository claims an enforcement mechanism that does not exist.</done>
</task>

</tasks>

<threat_model>
ASVS level: **1**. Blocking threshold: **high**.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor → CI | A pull request may introduce a second error-construction path; CI is the only automated reader |
| storage-layer error → RPC client | `*protovalidate.ValidationError` is D-01's public error surface and crosses to the caller via `runtime.MapError` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07-01 | Tampering | `*protovalidate.ValidationError` construction across the repo | medium | mitigate | Task 1's `check-single-validationerror-site` target makes a second production construction site a build failure; a divergent construction site is how a caller learns which layer rejected a write, which SC2 promises it cannot |
| T-03-07-02 | Repudiation | `mixinforproto/violation_test.go` doc comment | medium | mitigate | Task 2 replaces a comment asserting an enforcement mechanism that does not exist with a reference to the real target — a false enforcement claim causes reviewers to skip the manual check they would otherwise perform |
| T-03-07-03 | Denial of Service | The gate itself, run in CI | low | accept | A source-text grep over the repository is bounded by tree size and adds no network or module resolution; it cannot be made slow by attacker input |
| T-03-07-SC | Tampering | Package-manager installs | low | accept | No npm/pip/cargo install and no new Go module requirement — the gate is shell and `grep` only. `make check-dep-parity` (VAL-11/D-15) remains the standing control on module-version skew |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Symbol / path |
|---|---|
| Makefile target | `check-single-validationerror-site` (plus its `.PHONY` entry) |
| pipeline step | a new named step in `scripts/pipeline.sh` invoking the target |
| CI step | a new step in `.github/workflows/ci.yml` invoking the target |
| corrected comment | `mixinforproto/violation_test.go` lines 262-269, naming the real target |

Sibling plans create `proto/mixinforprototest/v1/ignore.proto`, `isZeroForKind`,
`fieldEvaluator.ignore`, the `MessageRules` carrier helpers, and the declaration-surface
exhaustiveness guards — see `03-06-PLAN.md` and `03-08-PLAN.md`.
</artifacts_this_phase_produces>

<flagged_assumptions>
This plan authors **one** prohibition from the §B recall/precision pass (the
guard-weakening prohibition, descriptor-less per the fallback divergence rule). The
full 30-item edge-probe accounting and the canon-referral breadcrumbs live in
`03-06-PLAN.md`'s `<flagged_assumptions>` block — recorded once for the plan set, not
duplicated, so the no-silent-drop equality is auditable in one place.

One assumption specific to this plan: the gate asserts a **source-text** invariant, not a
type-system one. A construction routed through a helper in another package, or via
reflection, would not be caught. That is accepted — the realistic regression is a
contributor writing the composite literal inline, which is exactly what the verifier's
rationale names.
</flagged_assumptions>

<verification>
- `make check-single-validationerror-site` exits 0 on the working tree and non-zero on a
  scratch copy carrying a planted second production construction site.
- `bash -n scripts/pipeline.sh` exits 0 and the header's numbered step list matches the
  steps executed.
- `make test` still exits 0 across both modules (the only Go file touched is a comment).
- `03-VERIFICATION.md` truth #8 — "VAL-07's single-`ValidationError`-construction-site
  invariant is enforced by CI" — is now demonstrable by running one command.
</verification>

<success_criteria>
- WR-07 closed: the invariant has a runnable, three-way-wired gate.
- No comment in the repository claims an enforcement mechanism the repository lacks.
- Both gate directions (clean pass, planted failure) are rehearsed and recorded.
- No Go production code and no `go.mod` is touched by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-validation-fidelity/03-07-SUMMARY.md` when done, recording
both rehearsal outputs verbatim, the chosen CI job and the reason the `standalone` job was
not used, and the exact exclusion rules the gate applies.
</output>
