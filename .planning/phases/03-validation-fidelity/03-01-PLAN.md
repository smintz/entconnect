---
phase: 03-validation-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mixinforproto/go.mod
  - mixinforproto/go.sum
  - mixinforproto/mixin.go
  - mixinforproto/hooks.go
  - mixinforproto/violation.go
  - mixinforproto/reverse.go
  - mixinforproto/doc.go
  - mixinforproto/README.md
  - mixinforproto/internal/difftest/ent/schema/residualcel.go
  - mixinforproto/internal/difftest/ent/entc.go
  - mixinforproto/internal/difftest/ent/generate.go
  - mixinforproto/internal/difftest/fakedriver.go
  - mixinforproto/internal/difftest/tracer_test.go
  - runtime/errormap.go
  - runtime/errormap_test.go
  - runtime/doc.go
  - .claude/CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [VAL-04, VAL-05, VAL-06, VAL-07, PIPE-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A Create mutation on a MixinForProto-derived field whose value fails a residual (buf.validate.field).cel rule is rejected before any SQL is issued, with an error that errors.As-matches *protovalidate.ValidationError (D-01/VAL-06)"
    - "The rejected mutation's violation carries the contract-authored RuleId (constraints.residual_cel.starts_with_x) and FieldPath value — the same RuleId and path protovalidate.Validate emits for the identical entity message (D-04/VAL-07)"
    - "A hook that finds no violations returns a nil error, never a non-nil *protovalidate.ValidationError with an empty Violations slice"
    - "For a string.max_len rule on a non-ASCII value, the hook's verdict follows protovalidate's code-point semantics rather than ent's byte-counting MinLen/MaxLen, proven by a value that passes one unit and fails the other"
    - "runtime.MapError translates a *protovalidate.ValidationError into connect.CodeInvalidArgument, matching connectrpc.com/validate's own mapping for the identical Go type reaching it at the boundary"
    - "mixinforproto/go.mod lists github.com/google/cel-go v0.28.0 in a require block with no // indirect marker, and mixinforproto compiles against import path github.com/google/cel-go/cel (D-16)"
    - "REQUIREMENTS.md VAL-05 and ROADMAP Phase 3 Success Criterion 1 state the amended scope (D-02/D-06), not the pre-amendment wording"
  prohibitions:
    - statement: "MUST NOT embed the rejected field's value in a violation message or in any server-side log line emitted by the mutation hook — a storage-layer rejection must name the rule and the field path, never the data"
      category: privacy
      requirement_id: VAL-06
      status: resolved
      verification: judgment
  artifacts:
    - path: "mixinforproto/hooks.go"
      provides: "The single mixin-declared Hooks() entry and its schema-load-time compiled state"
      contains: "func (m *protoMixin[M]) Hooks() []ent.Hook"
    - path: "mixinforproto/violation.go"
      provides: "The one shared *protovalidate.ValidationError constructor both evaluation paths feed"
      contains: "newValidationError"
    - path: "mixinforproto/reverse.go"
      provides: "ent Go value -> protoreflect.Value reverse conversion (string-scalar leg in this plan)"
      contains: "reverseValue"
    - path: "mixinforproto/internal/difftest/fakedriver.go"
      provides: "Hand-written dialect.Driver satisfying ent with zero new dependencies"
      contains: "fakeDriver"
    - path: "mixinforproto/internal/difftest/tracer_test.go"
      provides: "The end-to-end driverless proof that ent invokes the hook in a real Save() path"
    - path: "runtime/errormap.go"
      provides: "MapError's *protovalidate.ValidationError case"
      contains: "protovalidate.ValidationError"
  key_links:
    - from: "mixinforproto/mixin.go"
      to: "mixinforproto/hooks.go"
      via: "protoMixin[M].Hooks() overriding mixin.Schema's no-op default"
      pattern: "func \\(m \\*protoMixin\\[M\\]\\) Hooks\\(\\)"
    - from: "mixinforproto/hooks.go"
      to: "mixinforproto/violation.go"
      via: "both evaluation paths hand []*validate.Violation to newValidationError"
      pattern: "newValidationError\\("
    - from: "mixinforproto/internal/difftest/tracer_test.go"
      to: "mixinforproto/hooks.go"
      via: "a real generated ent.Client Save(ctx) driving the mixin hook through ent's own withHooks"
      pattern: "\\.Save\\(ctx\\)"
---

<objective>
Prove, end to end and in production-quality code, that a residual protovalidate CEL rule Tier 1
could not translate is now enforced at the storage layer with an error indistinguishable from the
one the boundary interceptor produces.

Purpose: Phase 1's D-11 left residual constraints recorded on `SourceField.ResidualIDs` and
enforced by nothing but Phase 2's boundary interceptor. This plan closes that gap along the
thinnest possible path — one message, one field, one custom CEL rule — through every layer the
phase will later expand: schema-load compilation, mutation-time evaluation, reverse conversion,
the shared violation constructor, ent's real `withHooks` pipeline, and `runtime.MapError`.

Output: `mixinforproto` gains its first `Hooks()` implementation and a direct cel-go dependency;
a driverless `internal/difftest` harness drives a real generated `ent.Client` through a real
`Save(ctx)`; `runtime.MapError` learns the new error type; and the two requirement-text
amendments this phase owes (ROADMAP SC1, VAL-05) plus the CLAUDE.md cel-go correction land before
any later plan is written against stale wording.
</objective>

## Phase Goal (verbatim from ROADMAP.md — not restated as a user story)

> Residual and message-level validation rules that Tier 1 can't translate get executed with
> byte-identical results at both the RPC boundary and the storage layer, proven by an automated
> differential harness

*(MVP mode is active but the ROADMAP `**Goal:**` line for Phase 3 is not in
`As a … I want to … so that …` form. Per the MVP planning rules a user story is NOT invented here.
Run `/gsd mvp-phase 03` if a user-story framing is wanted before execution.)*

## Artifacts this phase produces

New symbols, files and options created across Phase 3 (excluded from drift verification — none of
these exist yet):

**`mixinforproto` package**
- `hooks.go`, `violation.go`, `reverse.go`, `messagerules.go` (files)
- `func (m *protoMixin[M]) Hooks() []ent.Hook`
- `newValidationError`, `sortViolations`, `dedupeViolations` (violation.go)
- `reverseValue`, `errUnbindableKind`, `conversionError` (reverse.go)
- `hookState`, `fieldEvaluator`, `buildHookState` (hooks.go)
- `WithMessageRules` (Option), `MessageRuleTrigger` (type), `OnCreate` (constant)
- `SourceField.BoundaryOnlyIDs` (new annotation field, additive under ContractVersion 1)
- `fieldproj.SourceFieldProjection.BoundaryOnlyIDs`

**`mixinforproto/internal/difftest` package (new)**
- `fakeDriver`, `newTestClient`, `sweepSeedFor`, `generateValue`
- `ent/schema/residualcel.go`, `ent/schema/mixedrules.go`, `ent/schema/policyless.go`,
  `ent/schema/policybearing.go`, `ent/entc.go`, `ent/generate.go`, and the generated `ent` package

**`runtime` package**
- `MapError`'s `*protovalidate.ValidationError` case

**Root module test fixtures (new)**
- `internal/entconnecttest/hookwiring/` (schemas, generated `ent`, generated `entconnect`, tests)
- `proto/entconnecttest/v1/hookwiring.proto` and its generated stubs

**Build/CI**
- `Makefile` target `check-dep-parity`
- `.github/workflows/ci.yml` step in the `standalone` job invoking it

**Corpus (new proto messages, `mixinforprototest.v1`)**
- `MixedFieldRules`, `ReverseScalars`, `ReverseEnum`, `ReverseWkt`, `ReverseScalarMap`,
  `ReverseAsJSON`, `MessageRuleOk`, `MessageRuleExcludedRef` and their `testdata/*.golden` fixtures

<execution_context>
@/home/user/entconnect/.claude/gsd-core/workflows/execute-plan.md
@/home/user/entconnect/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-validation-fidelity/03-CONTEXT.md
@.planning/phases/03-validation-fidelity/03-RESEARCH.md
@.planning/phases/03-validation-fidelity/03-PATTERNS.md

Environment note: the Go toolchain is not on `PATH` by default in this sandbox. Run
`source /usr/local/use-go-1.25.1.sh` (or `use-go-1.24.7.sh`) first; the root module's
`go 1.26` directive triggers a `GOTOOLCHAIN` auto-download through the configured proxy,
which 03-RESEARCH.md confirmed works.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm D-01 — mixinforproto's public error surface becomes protovalidate's</name>
  <decision>Should the mutation-time hook return protovalidate's own `*protovalidate.ValidationError` as `mixinforproto`'s public, adopter-facing error type?</decision>
  <context>
D-01 in 03-CONTEXT.md records this choice and rates it **one-way**. It is the error type every
adopter's `errors.As` will target, and VAL-06 promises it as a structured error consumable
outside any RPC context — so once released it is a published contract, not an internal detail.
The recorded cost: a protovalidate major-version bump becomes a `mixinforproto` breaking change.

This checkpoint exists because REVERSIBILITY_GATES is enabled and the door is genuinely one-way,
not to re-open the decision. D-01 was made by the developer during `/gsd-discuss-phase`; the
expected answer is `option-a`. Selecting anything else invalidates the rest of this phase's
plan set and must be surfaced before Task 2 runs.
  </context>
  <options>
    <option id="option-a">
      <name>Return protovalidate's own *protovalidate.ValidationError (D-01, as recorded)</name>
      <pros>Identity with the boundary becomes a property of using the same object rather than two code paths agreeing to construct the same thing. `connectrpc.com/validate` already builds its `connect.Error` from this exact type. Costs mixinforproto no new dependency — `buf.build/gen/go/bufbuild/protovalidate/protocolbuffers/go` is already in its first require block with no `// indirect` marker.</pros>
      <cons>mixinforproto's public error surface is now protovalidate's; a protovalidate major bump becomes a mixinforproto breaking change.</cons>
    </option>
    <option id="option-b">
      <name>Wrap violations in a mixinforproto-owned error type</name>
      <pros>Insulates adopters from protovalidate major bumps; mixinforproto controls its own error contract.</pros>
      <cons>Re-introduces exactly the "two code paths agreeing to construct the same thing" failure VAL-07 exists to eliminate; every adopter must unwrap twice; `runtime.MapError` and `connectrpc.com/validate` would classify the two layers' errors differently. Rejects D-01 and invalidates plans 03-02 through 03-05.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: End-to-end — one residual CEL rule rejects a real ent Create, driverless</name>
  <reversibility rating="one-way">D-01: `*protovalidate.ValidationError` becomes mixinforproto's published error type — every adopter's `errors.As` targets it, so changing it later breaks the contract VAL-06 promises outside any RPC context.</reversibility>
  <precondition>`source /usr/local/use-go-1.25.1.sh` puts `go` on PATH and `go env GOMODCACHE` resolves; `github.com/google/cel-go v0.28.0` is already present in `mixinforproto/go.sum` (it is an existing indirect dependency, so no new module download is required).</precondition>
  <files>mixinforproto/go.mod, mixinforproto/go.sum, mixinforproto/mixin.go, mixinforproto/hooks.go, mixinforproto/violation.go, mixinforproto/reverse.go, mixinforproto/internal/difftest/ent/schema/residualcel.go, mixinforproto/internal/difftest/ent/entc.go, mixinforproto/internal/difftest/ent/generate.go, mixinforproto/internal/difftest/fakedriver.go, mixinforproto/internal/difftest/tracer_test.go</files>
  <read_first>
    - `mixinforproto/mixin.go` — `protoMixin[M]` embeds `mixin.Schema`, whose no-op `Hooks()` this task overrides; the doc comment on line 10 explicitly anticipates this change.
    - `mixinforproto/validate.go` — its file doc comment (lines 17-56) establishes that `buf.build/gen/.../buf/validate` is ALREADY a direct, non-indirect dependency (D-01 is load-bearing on this), and `delegatingFormatValidator` (lines 401-431) is the closest existing precedent for building a synthetic `dynamicpb.Message`, handing it to a real `protovalidate.Validator`, and filtering violations down to one field.
    - `mixinforproto/fieldmap.go` — `resolvedFieldRules` (lines ~195-226) is the exact `protovalidate.ResolveFieldRules` call site D-03 reuses; `celRuleResidual`/`residualEntry` already carry the CEL expression strings this task compiles.
    - `mixinforproto/errors.go` — `failure`/`derivationError`/`newDerivationError`: schema-load failures go through this machinery and nothing else. The new runtime violation constructor sits BESIDE it, never replaces it.
    - `mixinforproto/annotation.go` — `SourceField.ResidualIDs`/`ResidualFingerprint`, the provenance this phase's enforcement finally acts on.
    - `mixinforproto/internal/boundarytest/boundary_test.go` — the no-DB-driver harness discipline `internal/difftest` is a sibling of.
    - `proto/mixinforprototest/v1/constraints.proto` lines 166-176 — the `ResidualCel` fixture message this tracer drives.
    - `mixinforproto/testdata/constraints_residual_cel.golden` — proof the residual-recording pipe already works end to end.
    - `internal/entconnecttest/update/ent/entc.go` and `internal/entconnecttest/update/ent/generate.go` — the committed `//go:generate go run entc.go` fixture-codegen pattern to mirror (WITHOUT the entconnect extension: `mixinforproto` must not import the root module).
    - `mixinforproto/go.mod` — `github.com/google/cel-go v0.28.0` currently sits in the second (`// indirect`) require block.
    - `03-RESEARCH.md` "Code Examples" — the verified call shapes for `protovalidate.New(WithMessages, WithDisableLazy)`, the local `cel.Env` construction, the shared violation constructor, and the `fakeDriver` interface surface.
  </read_first>
  <action>
Wire ONE residual-CEL path through every layer this phase will later expand, production-quality,
no other call sites and no batching.

Promote cel-go to a direct dependency (D-07 consequence 2, D-16): move
`github.com/google/cel-go v0.28.0` out of `mixinforproto/go.mod`'s `// indirect` block into the
first require block and drop the `// indirect` marker. The import path is
`github.com/google/cel-go/cel` — NOT `github.com/cel-expr/cel-go/cel`, which is not a dependency
of `buf.build/go/protovalidate@v1.2.0` and does not resolve. Do not change the version. Keep
`mixinforproto/go.mod`'s `go 1.24.0` directive exactly as-is; the Makefile's `check-goversion`
target asserts it byte-for-byte.

Create `mixinforproto/violation.go` with the single shared constructor D-07 consequence 3
requires: one function taking `[]*validate.Violation` and returning
`*protovalidate.ValidationError` by wrapping each into `&protovalidate.Violation{Proto: ...}`.
Both evaluation paths must funnel through this one function; the residual/CEL path must never
grow its own error-building code. Give the file a doc comment distinguishing schema-load
collected failures (`errors.go`'s `failure`/`derivationError`) from mutation-time protovalidate
violations (this file). Naming `*validate.Violation` here requires importing
`buf.build/gen/go/bufbuild/protovalidate/protocolbuffers/go/buf/validate`, which is already a
direct, non-indirect dependency — read `validate.go`'s file doc comment for why that is not a
new dependency-surface cost.

Create `mixinforproto/reverse.go` with the string-scalar leg of D-05's reverse table only:
one exported-to-package function converting an ent mutation's Go value plus the field's
`protoreflect.FieldDescriptor` and its `SourceField.Kind` derivation-class string into a
`protoreflect.Value`. For this tracer implement the `"scalar"` case for `protoreflect.StringKind`
and return a plain, non-protovalidate `error` for every other class, naming the class and the
field. Per D-12 that error is a data-integrity fault, not a constraint violation: never wrap it
in `*protovalidate.ValidationError` and never synthesize a RuleId. Keep this file adjacent to
`fieldmap.go` conceptually — its doc comment must state that it is the mirror of `fieldmap.go`'s
forward table so a change to one is visibly a change to the other (D-05).

Create `mixinforproto/hooks.go` with the single mixin-declared `Hooks()` entry (VAL-04) and its
schema-load-time compiled state. At `Hooks()` construction time — never inside the returned hook
closure — walk the message descriptor, call `protovalidate.ResolveFieldRules(fd)` per field
(reusing `fieldmap.go`'s existing resolution path per D-03), and for every
`(buf.validate.field).cel` rule build ONE `cel.Env` from `cel.Lib(pvcel.NewLibrary())` plus
`pvcel.RequiredEnvOptions(fd)...` plus a `cel.Variable("this", ...)` binding, then `Compile` and
`Program` the expression once. Alias the protovalidate CEL subpackage as `pvcel`. At mutation
time the returned `ent.Hook` reads the in-scope field set from `mutation.Fields()`, reverse-
converts each value, evaluates the compiled programs, collects `*validate.Violation` protos, and
returns the shared constructor's `*protovalidate.ValidationError` — or `nil` when there are no
violations. A compile failure at schema-load time must become a `failure{rule: "hook"}` fed
through `newDerivationError` (D-09's uncompilable-CEL panic path), never a second bare-panic
mechanism. Violation messages must name the rule id and field path only — never the rejected
value, and the hook must emit no log line carrying the value.

Override `Hooks()` on `protoMixin[M]` in `mixinforproto/mixin.go`, following the existing
`Fields()`/`Annotations()` adapter shape: the pure core returns collected failures and this one
adapter method panics.

Create `mixinforproto/internal/difftest` as a sibling of `internal/boundarytest`: an ent schema
package declaring `mixinforproto.MixinForProto[*mixinforprototestv1.ResidualCel]()`, an
`ent/entc.go` build-ignored codegen entry point calling `entc.Generate("./schema", &gen.Config{})`
with NO extension (mixinforproto must not import the root module), an `ent/generate.go` carrying
the `//go:generate go run entc.go` directive, the committed generated `ent` package, and a
hand-written `fakeDriver` satisfying `entgo.io/ent/dialect.Driver`'s five methods (`Exec`,
`Query`, `Tx`, `Close`, `Dialect`) with zero new imports. Adding a DB driver to
`mixinforproto/go.mod` is forbidden — that property is exactly what D-13 protects.

Write `tracer_test.go` driving a real generated `ent.Client` built on `fakeDriver`:
`Create().SetValue("Y").Save(ctx)` must be rejected before any driver call, and
`Create().SetValue("Xyz").Save(ctx)` must reach the driver. In the same test file assert
Research Open Question 1 / Pitfall 2 empirically: inside the hook, on a Create where the caller
never called the setter for a `Default()`-bearing derived field, record whether that field
appears in `mutation.Fields()`. State the observed answer in the test's failure message and in
the SUMMARY, because D-06's mechanism in plan 03-03 collapses to one code path with no `Op()`
branch if it holds, and needs the two-path design if it does not.
  </action>
  <verify>
    <automated>cd mixinforproto && go test ./internal/difftest/... -count=5 -race</automated>
    <automated>cd mixinforproto && GOWORK=off go build ./... && GOWORK=off go test ./...</automated>
    <automated>make check-goversion && make check-modules</automated>
  </verify>
  <acceptance_criteria>
    - `cd mixinforproto && go test ./internal/difftest/... -count=5 -race` exits 0.
    - `mixinforproto/go.mod` contains `github.com/google/cel-go v0.28.0` inside a require block with no `// indirect` marker on that line, and `go mod edit -json mixinforproto/go.mod` reports `"Indirect"` false for that module.
    - `mixinforproto/go.mod` still declares `go 1.24.0`; `make check-goversion` exits 0.
    - `mixinforproto/go.mod` contains no `modernc.org/sqlite`, no `github.com/mattn/go-sqlite3`, and no `github.com/lib/pq` entry in any require block.
    - A test asserts that `client.ResidualCel.Create().SetValue("Y").Save(ctx)` returns an error for which `errors.As(err, &ve)` with `ve *protovalidate.ValidationError` is true.
    - That same test asserts `len(ve.Violations) == 1`, `ve.Violations[0].Proto.GetRuleId() == "constraints.residual_cel.starts_with_x"`, and the violation's field path renders as `value`.
    - A test asserts `client.ResidualCel.Create().SetValue("Xyz").Save(ctx)` returns nil error (the fake driver accepts the write).
    - A test asserts the hook returns a `nil` error — not a non-nil `*protovalidate.ValidationError` with zero violations — for a passing mutation.
    - A test named for Open Question 1 records whether an unset `Default()`-bearing derived field appears in `mutation.Fields()` on Create, and its failure message states the observed answer verbatim.
    - `mixinforproto/hooks.go` builds every `cel.Program` outside the returned `ent.Hook` closure: a test asserts that constructing the mixin's `Hooks()` twice and running N mutations performs compilation exactly at construction (e.g. by counting compilations through a package-level test seam or by asserting `Hooks()` returns a closure over already-built programs).
    - `go mod edit -json mixinforproto/go.mod` reports exactly one require entry whose `Path` ends in `/cel-go`, and that `Path` is `github.com/google/cel-go`.
    - `cd mixinforproto && GOWORK=off go build ./... && GOWORK=off go test ./...` exits 0.
  </acceptance_criteria>
  <done>A real generated `ent.Client` Create is rejected at the storage layer by a residual protovalidate CEL rule, with a `*protovalidate.ValidationError` carrying the contract's own RuleId and field path, proven without any database driver in `mixinforproto`'s require block — and the Create-side `mutation.Fields()` question is answered empirically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the wire error and every stale "until Phase 3" claim true</name>
  <files>runtime/errormap.go, runtime/errormap_test.go, runtime/doc.go, mixinforproto/doc.go, mixinforproto/README.md, .claude/CLAUDE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `runtime/errormap.go` — the full `MapError` switch (lines 38-52) and its doc comment (lines 11-37), which explains precisely which error types can and cannot live in this shared package. The new case needs a fourth bullet written in that same justification style.
    - `runtime/doc.go` line 12 — the "Until Phase 3 lands, this package's protovalidate interceptor stage is the ONLY thing enforcing untranslated protovalidate constraints" note, which becomes false the moment Task 2 lands.
    - `mixinforproto/README.md` lines 150 and 158-161 — "This release ships no hook (Tier 2 CEL …)" and "recorded on each field's `SourceField` annotation, but not enforced at the storage layer".
    - `mixinforproto/doc.go` — the closing paragraph on mixin hook ordering and "what this release does and does not enforce".
    - `.claude/CLAUDE.md` — the Descriptor/Validation Toolchain table row for cel-go, the "What NOT to Use" row for `github.com/google/cel-go`, and the Version Compatibility row pairing protovalidate with cel-go.
    - `.planning/REQUIREMENTS.md` line 40 (VAL-05) — the wording D-06 amends.
    - `.planning/ROADMAP.md` line 118 (Phase 3 Success Criterion 1) — the wording D-02 amends.
    - `.planning/phases/02-crud-handlers-interceptor-chain/02-CONTEXT.md` D-18 — the error-mapping table this new case slots into.
  </read_first>
  <behavior>
    - Test 1: `MapError` given a `*protovalidate.ValidationError` returns a `*connect.Error` whose `connect.CodeOf` is `connect.CodeInvalidArgument`.
    - Test 2: `MapError` given an error that WRAPS a `*protovalidate.ValidationError` (via `fmt.Errorf("%w", …)`) still returns `CodeInvalidArgument` — the case must use `errors.As`, not a bare type assertion.
    - Test 3: `MapError` given `privacy.Deny` still returns `CodePermissionDenied` — the new case must not be ordered ahead of the existing `errors.Is(err, privacy.Deny)` branch.
    - Test 4: `MapError` given an already-typed `*connect.Error` still returns it unchanged.
    - Test 5: `MapError` given an unrecognised error still returns `CodeInternal` with a generic message.
    - Test 6: the returned `*connect.Error` for a `*protovalidate.ValidationError` carries the ValidationError as its underlying error, so an adopter can `errors.As` back to it.
  </behavior>
  <action>
Add the `*protovalidate.ValidationError` case to `runtime.MapError`, placed BETWEEN the existing
`errors.Is(err, privacy.Deny)` branch and the `errors.As(err, &connectErr)` branch, mapping to
`connect.CodeInvalidArgument` with the ValidationError itself as the wrapped error. This matches
`connectrpc.com/validate`'s own mapping for the identical Go type reaching it at the boundary,
which is what makes VAL-07's identity guarantee concrete in code. Add
`"buf.build/go/protovalidate"` to this file's import block (`runtime/interceptor.go` already
imports it, so the module is a resolved dependency of `runtime`; only this file lacks the
import). Extend the file's doc comment with a fourth bullet explaining, in the same style used
for `privacy.Deny` and `*connect.Error`, why protovalidate's error type is generic rather than
application-specific and therefore CAN live in the shared package — unlike ent's per-application
generated `*NotFoundError`/`*ConstraintError`/`*ValidationError`. This is Claude's Discretion
item 2 from 03-CONTEXT.md; without it a residual violation reaching `MapError` unhandled would
surface as `Internal` and silently break VAL-07.

Correct `runtime/doc.go`'s stale bullet: the storage layer is no longer a gap, and the sentence
must now say that both the boundary interceptor stage and `mixinforproto`'s mutation hook enforce
protovalidate constraints, with the boundary remaining the only enforcement point for
message-level (cross-field) rules unless a schema opts in with `WithMessageRules(OnCreate)`
(VAL-08). Do not delete the bullet — restate it truthfully.

Correct `mixinforproto/README.md`'s "not enforced at the storage layer" language and
`mixinforproto/doc.go`'s "what this release does and does not enforce" clause the same way:
field-scoped rules are now enforced at both layers with identical verdicts (D-02); message-level
rules remain boundary-only by default. Point readers at `ResidualIDs` for provenance rather than
for "what isn't enforced yet".

Correct `.claude/CLAUDE.md` per D-16 and 03-RESEARCH.md Summary point 1, which verified this
against the downloaded module source: the cel-go row must read `github.com/google/cel-go` at
`v0.28.0`, because that is exactly what `buf.build/go/protovalidate@v1.2.0`'s own `go.mod`
requires and what both of this repo's `go.mod` files already resolve. Remove
`github.com/cel-expr/cel-go` from the "What NOT to Use" → "Use Instead" column and instead record
the inverse fact: `cel-expr/cel-go` is not a dependency of the pinned protovalidate and its own
`go.mod` still declares the old path, so importing it produces a `cel.Library` type mismatch at
compile time. Update the Version Compatibility row to pair
`buf.build/go/protovalidate@v1.2.0` with `github.com/google/cel-go@v0.28.0`. Also update the
"Stack Patterns by Variant" bullet that names `github.com/cel-expr/cel-go/cel` in mixinforproto's
dependency set.

Amend the two requirement texts this phase owes, following the Phase 2 precedent (CRUD-03's
`Paginate()` correction, CRUD-07's "only place constructed" correction) of amending in-phase
rather than silently diverging. `.planning/REQUIREMENTS.md` VAL-05 currently says the hook
evaluates only fields changed by the mutation; per D-06 it must say the scope is
operation-dependent — all derived fields on Create (because a derived field carries
`Default(zero)`, so an unset field on Create still persists as a real value the boundary
validates), changed-only on Update. `.planning/ROADMAP.md` Phase 3 Success Criterion 1 currently
scopes the hook to residual CEL rules only; per D-02 it must say the hook evaluates the full
protovalidate field-rule set for every in-scope field, translated and residual alike, because a
Tier 1 translated rule failing at the storage layer produces ent's own `*ValidationError`
carrying no protovalidate constraint ID, which makes VAL-07 unachievable under a residual-only
hook. Add a dated amendment note under each, naming the decision ID, exactly as Phase 1's and
Phase 2's planning notes do.
  </action>
  <verify>
    <automated>go test ./runtime/... -count=5 -race</automated>
    <automated>go build ./... && go vet ./runtime/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./runtime/... -count=5 -race` exits 0 and includes a test asserting `connect.CodeOf(runtime.MapError(&protovalidate.ValidationError{...})) == connect.CodeInvalidArgument`.
    - A test asserts the same result for an error wrapped with `fmt.Errorf("%w", ve)`, proving `errors.As` semantics.
    - A test asserts `runtime.MapError(privacy.Deny)` still yields `connect.CodePermissionDenied`.
    - A test asserts an unrecognised error still yields `connect.CodeInternal`.
    - `runtime/errormap.go` imports `buf.build/go/protovalidate` and its doc comment has a fourth bullet naming `*protovalidate.ValidationError`.
    - `runtime/doc.go` no longer contains the string `Until Phase 3 lands`.
    - `.claude/CLAUDE.md` contains `github.com/google/cel-go` with version `v0.28.0` in the Descriptor/Validation Toolchain table, and `grep -c "cel-expr/cel-go@v0.31.0" .claude/CLAUDE.md` returns 0.
    - `.planning/REQUIREMENTS.md`'s VAL-05 line contains the word `Create` and the word `Update` and cites `D-06`.
    - `.planning/ROADMAP.md` Phase 3 Success Criterion 1 no longer contains the phrase `only checks fields the mutation actually changed` and cites `D-02`.
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>A `*protovalidate.ValidationError` from either layer maps to the same `CodeInvalidArgument` wire error; no shipped doc, README, package comment, or requirement text still claims the storage layer does not enforce residual constraints; and CLAUDE.md names the cel-go module path that actually resolves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Connect request → generated handler | Untrusted client input crosses here; Phase 2's protovalidate interceptor is the first gate. |
| Handler → ent mutation | Application-shaped values cross into the storage layer; this phase makes the mixin hook the second gate. |
| ent mutation value → `protoreflect.Value` | Reverse conversion; a value that will not bind is a data-integrity fault, not a constraint failure. |
| Violation → wire error | `runtime.MapError` decides what a caller learns about a rejection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `hooks.go` violation construction and any log line it emits | medium | mitigate | Violation messages carry rule id + field path only; the hook emits no log line containing the rejected value (see the plan's `must_haves.prohibitions` entry). |
| T-03-02 | Tampering | `runtime.MapError` default branch | high | mitigate | Task 3 adds the explicit `*protovalidate.ValidationError` → `CodeInvalidArgument` case; without it a storage-layer violation degrades to `CodeInternal`, silently breaking VAL-07's identity guarantee and letting a caller distinguish the two layers. |
| T-03-03 | Repudiation | `reverse.go` D-12 error path | high | mitigate | A reverse-conversion failure returns a plain non-protovalidate `error` with no synthesized RuleId; fabricating one would put a false constraint ID on the wire and corrupt PIPE-06's signal. |
| T-03-04 | Denial of Service | per-request CEL compilation | medium | mitigate | Every `cel.Program` is compiled at `Hooks()` construction (schema load), never inside the hook closure; asserted by an acceptance criterion. |
| T-03-05 | Elevation of Privilege | mixin hook vs `privacy.Policy` ordering | low | accept | 03-RESEARCH.md verified `Hooks[0]` is the combined privacy Policy whenever any Policy is declared, so authorization precedes validation. Plan 03-04 pins this with a test; no code change is owed here. |
| T-03-SC | Tampering | Go module dependency promotion (`github.com/google/cel-go`) | high | mitigate | No new external package name enters the stack: cel-go is already an indirect dependency at the identical version in both `go.mod` files and is exactly what `buf.build/go/protovalidate@v1.2.0` itself requires (verified against downloaded module source, 03-RESEARCH.md Package Legitimacy Audit — verdict OK, no `[ASSUMED]`/`[SUS]`/`[SLOP]` package). The Go ecosystem is outside the npm/pypi/crates legitimacy-gate seam, so the stronger source-read verification stands in its place. |
</threat_model>

<verification>
- Both modules build and vet clean: `make build && make vet`.
- Both modules test clean, five times: `make test-determinism`.
- `mixinforproto` still resolves standalone: `make test-standalone`.
- No committed `go.mod` gains a `replace` directive: `make check-modules`.
- `mixinforproto/go.mod`'s `go` pin is unchanged: `make check-goversion`.
</verification>

<success_criteria>
- A residual `(buf.validate.field).cel` rule rejects a real ent Create at the storage layer with a `*protovalidate.ValidationError` carrying the contract's RuleId and field path.
- `mixinforproto` has a `Hooks()` implementation, a direct cel-go dependency, and still no DB driver.
- Both evaluation paths (only one exists yet) construct their error through exactly one function.
- `runtime.MapError` maps the new type to `CodeInvalidArgument`.
- Every doc and requirement statement that said the storage layer does not enforce residual constraints now says something true.
</success_criteria>

<output>
Create `.planning/phases/03-validation-fidelity/03-01-SUMMARY.md` when done. Record the observed
answer to Open Question 1 (`mutation.Fields()` content on Create) explicitly — plan 03-03's D-06
task branches on it.
</output>
